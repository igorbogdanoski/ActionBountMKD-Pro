rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only data-fidelity drill. Authorization is tested separately
    // by firebase.rules.test.json against the production firestore.rules file.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
