{
  "name": "actionbountmkd-pro",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces"
  },
  "dependencies": {
    "is-arrayish": "^0.3.4"
  }
}
